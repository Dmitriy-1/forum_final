<header>
    <div class="headerContent">
        <div class="textHeader">
            <a class="Header_logo" href="{{ path('about') }}">Forum</a>
        </div>

        <div class="buttonContent">
            <div class="nameHeader">

                {% if app.user %}
                    Здравствуйте,{{ app.user.username     }}
                    {% if is_granted('ROLE_ADMIN')%}
                        <a class="button" href="{{ path('easyadmin') }} ">Админка</a>
                    {% else %}
                        <a class="button"  href="{{ path('topic_new') }}">Создать</a>
                {% endif %}
                    <a class="button"  href="{{ path('topic_index') }}">Топики</a>
                    <a class="button" href="{{ path('app_logout') }}">Выйти</a>
                {% else %}
                    <a class="button"  href="{{ path('topic_index') }}">Топики</a>
                    <a class="button" href="{{ path('app_register') }}">Регистрация</a>
                    <a class="button" href="{{ path('app_login') }}">Войти</a>
                {% endif %}
            </div>

        </div>
    </div>
</header>